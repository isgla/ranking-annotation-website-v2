This app has to parts:
- Frontend 
- Backend

To have both the backend and frontend running, follow the next steps:

conda create --name rankingUI python=3.11
conda activate rankingUI
conda install -c conda-forge nodejs

node -v
npm -v

### `npm install`
### `npm run build`

### `cd server`
### `node index.js`